# frames.js export fix

This is a repository shown the export fix for [frames.js](https://github.com/framesjs/frames.js)

Steps:

```shell
yarn i && yarn build
ls -l dist/
```

You can compare the bundle size of before.mjs and after.mjs
